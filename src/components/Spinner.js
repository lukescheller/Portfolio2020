import React from 'react'
import spinner from '../images/spinner.gif'

const Spinner = () => {
    return (
        <div>
            <img src={spinner} alt='' />
        </div>
    )
}

export default Spinner